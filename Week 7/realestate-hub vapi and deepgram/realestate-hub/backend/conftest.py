"""Pytest auto-discovers this file and adds this directory (backend/) to
sys.path, so `tests/*.py` can do `from config import ...`,
`from voice_agent import ...`, etc. without a package/src layout.
"""
